<% provide(:title, 'All users') %>
<h1>All users</h1>
<div class="centered">
<%= will_paginate %>
</div>
<ul class="users">
  <%= render @users %>
</ul>

<div class="centered">
<%= will_paginate %>
</div>